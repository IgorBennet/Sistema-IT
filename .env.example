# URL real fornecida pelo sistema corporativo para “Voltar ao Menu Principal”.
NEXT_PUBLIC_MAIN_MENU_URL=
# Troque para "api" quando a implementação real estiver disponível.
NEXT_PUBLIC_DATA_SOURCE=mock
# URL base esperada para a futura API; não usada no modo mock.
NEXT_PUBLIC_API_BASE_URL=
