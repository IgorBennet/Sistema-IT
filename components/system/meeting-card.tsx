import{CalendarDays,Download,Pin}from"lucide-react";import type{KnowledgeItem}from"@/types/item";import{formatDate}from"@/utils/format";import{TypeBadge}from"./type-badge";
export function MeetingCard({item,onOpen,onDownload}:{item:KnowledgeItem;onOpen:()=>void;onDownload:()=>void}){const date=new Date(item.createdAt);return <article className="surface flex min-h-56 flex-col rounded-xl p-4 transition hover:-translate-y-0.5 hover:shadow-lg"><div className="flex gap-4"><div className="flex h-20 w-16 shrink-0 flex-col items-center justify-center rounded-lg bg-[#eff7ff] text-[#0a417a]"><strong className="text-xl">{String(date.getDate()).padStart(2,"0")}</strong><span className="text-xs font-bold uppercase">{date.toLocaleDateString("pt-BR",{month:"short"}).replace(".","")}</span><span className="text-xs">{date.getFullYear()}</span></div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><button onClick={onOpen} className="text-left font-bold text-[#082c55] underline-offset-4 hover:underline">{item.title}</button>{item.important&&<span title="Item importante" className="inline-flex items-center gap-1 rounded-md border border-red-200 bg-red-50 px-2 py-1 text-xs font-bold text-red-700"><Pin className="size-3"/>Importante</span>}</div><div className="mt-2"><TypeBadge type="meeting"/></div></div></div><p className="mt-4 line-clamp-2 text-sm leading-6 text-[#445e79]">{item.description}</p><div className="mt-auto flex items-end justify-between gap-3 pt-4 text-sm text-[#506781]"><div className="flex items-center gap-2"><span className="flex size-9 items-center justify-center rounded-full bg-[#173e6e] text-xs font-bold text-white">{item.author.initials}</span><span><strong className="block text-[#183958]">{item.author.name}</strong><span className="text-xs">Criado em {formatDate(item.createdAt)}</span></span></div><button onClick={onDownload} disabled={!item.attachment} className="inline-flex min-h-11 min-w-11 items-center justify-center rounded-lg hover:bg-[#eaf4ff] disabled:opacity-35" aria-label={item.attachment?`Baixar ${item.attachment.name}`:"Sem anexo"}>{item.attachment?<Download className="size-5"/>:<CalendarDays className="size-5"/>}</button></div></article>}
