"use client";
import{Download,FileText}from"lucide-react";import{Dialog,DialogContent,DialogDescription,DialogHeader,DialogTitle}from"@/components/ui/dialog";import type{KnowledgeItem}from"@/types/item";
export function AttachmentDialog({item,open,onOpenChange,onDownload}:{item:KnowledgeItem|null;open:boolean;onOpenChange:(value:boolean)=>void;onDownload:()=>void}){if(!item?.attachment)return null;return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="sm:max-w-3xl"><DialogHeader><DialogTitle>Visualização do anexo</DialogTitle><DialogDescription>{item.attachment.name}</DialogDescription></DialogHeader>{item.attachment.url&&item.attachment.mimeType.startsWith("image/")?<img src={item.attachment.url} alt={`Anexo de ${item.title}`} className="max-h-[60vh] w-full rounded-xl border object-contain"/>:<div className="flex min-h-80 flex-col items-center justify-center rounded-xl border border-dashed border-[#9ab5d0] bg-[#f4f9ff] p-8 text-center"><FileText className="size-14 text-[#0a68be]"/><h3 className="mt-4 text-lg font-bold">Prévia demonstrativa</h3><p className="mt-2 max-w-md text-[#52677e]">A visualização segura do arquivo será conectada ao endpoint de anexos do backend. O download mockado já está disponível.</p></div>}<button onClick={onDownload} className="ml-auto inline-flex min-h-11 items-center justify-center gap-2 rounded-lg bg-[#0768c6] px-4 font-bold text-white"><Download className="size-5"/>Baixar arquivo</button></DialogContent></Dialog>}
